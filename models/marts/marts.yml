version: 2

models:
  - name: dim_customers
    columns:
      - name: user_id
        tests: [not_null, unique]

  - name: fct_orders
    columns:
      - name: order_id
        tests: [not_null, unique]
      - name: order_amount
        tests:
          - not_null
